<?xml version="1.0" encoding="UTF-8" ?>
<FlyoutPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:controls="clr-namespace:eVehicleStartupLog.Controls"
    x:Class="eVehicleStartupLog.Views.MainFlyoutPage"
    Title="eVehicleStartupLog">
    <FlyoutPage.Flyout>
        <ContentPage Title="Flyout">
            <StackLayout>
                <controls:MenuButton Text="Alkalmazottak"
                                     Command="{Binding Path=OpenEmployeesCommand}"/>
                <controls:MenuButton Text="Rendszámok"
                                     Command="{Binding Path=OpenPlatesCommand}"/>
                <controls:MenuButton Text="Járművek"
                                     Command="{Binding Path=OpenVehiclesCommand}"/>
                <controls:MenuButton Text="Korábbi utak"
                                     Command="{Binding Path=OpenPreviousTripsCommand}"/>
            </StackLayout>
        </ContentPage>
    </FlyoutPage.Flyout>
    <FlyoutPage.Detail>
        <ContentPage Title="Detail">

        </ContentPage>
    </FlyoutPage.Detail>
</FlyoutPage>
